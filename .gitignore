node_modules/
.next/
out/
dist/
build/
.cache/
.env*
!.env.example
.vercel
next-env.d.ts
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

/generated/prisma

# Base SQLite locale de développement (voir docs/decisions.md)
*.db
*.db-journal

# Caches d'outils d'assistance au développement (pas du code source du projet)
/.agents/
/.claude/
/.windsurf/
/skills-lock.json
